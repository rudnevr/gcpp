//
// Created by KR on 11/29/2017.
//

#include "Keys.h"

Keys::Keys(bool caps, bool shift, bool alt, bool ctrl, bool tab, bool win) : caps(caps), shift(shift), alt(alt),
                                                                             ctrl(ctrl), tab(tab), win(win) {}
